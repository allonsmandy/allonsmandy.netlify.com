import styled from "styled-components"

export const ReadingsWrapper = styled.article`
  background-color: var(--secondary);
  border-radius: 4px;
  margin: 0 1.4rem 1.4rem 1.4rem;
  padding: 0.8rem;
`

export const ReadingsTitlePage = styled.h1`
  color: var(--texts);
`

export const ReadingsDescriptionPage = styled.p`
  color: var(--texts);
  padding: 0 1.4rem !important;
  margin-bottom: 1.4rem;
`

export const ReadingsTitle = styled.h2`
  color: var(--background);
  font-size: 1rem;
  padding: 0;
  margin-bottom: 5px;
`

export const ReadingsDescription = styled.p`
  color: var(--background);
`
